version: 2
updates:
  # The frp version tracks upstream releases on its own; this only keeps the
  # actions used by the build workflow current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
